---
import Card from '@components/atoms/Card.astro'
interface Props {
  id: string
  class?: string
}
const { id, class: customClass } = Astro.props
---

<Card class={customClass}>
  <iframe
    class="w-full border-none h-20 sm:h-[232px]"
    title="Podcasts Frijol Mágico"
    src={`https://open.spotify.com/embed/episode/${id}?utm_source=generator&t=0`}
    allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
    loading="lazy"></iframe>
</Card>
